import styled from 'styled-components';

import { Container } from '@Atoms/Container';

export const PolicyContainer = styled(Container)`
    padding: 16px;
    gap: 16px;
`;

export const PolicyBox = styled(Container)`
    gap: 8px;
`;
