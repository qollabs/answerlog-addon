import { Container } from '@Atoms/Container';
import { GRAY0_BACKGROUND } from '@Styles/themes';
import styled from 'styled-components';

export const TestContainer = styled(Container)`
    padding: 16px;
`;
